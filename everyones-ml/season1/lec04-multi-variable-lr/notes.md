# Lec 4 — 여러 개의 입력(feature)의 Linear Regression / Lab1, Lab2

## 핵심 키워드
- Multi-variable hypothesis: H(X) = XW + b
- Matrix form
- 파일에서 데이터 로딩 (csv → numpy)

## 학습 메모

## 실습 메모 (TF 2.x)
- Lab 1: 행렬 곱으로 multi-variable LR
- Lab 2: `np.loadtxt` 또는 `pandas.read_csv`로 데이터 로딩
- 큰 파일은 `tf.data.Dataset`으로 배치 처리
