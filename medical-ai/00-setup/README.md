# 00 · Setup

## 목표
- Python / PyTorch / scikit-learn 환경 동작 확인
- Jupyter Lab 실행 확인
- 의료 AI에 쓸 라이브러리 import 체크

## 체크리스트
- [ ] `pip install -r ../requirements.txt` 실행
- [ ] `env_check.ipynb` 위에서 아래로 에러 없이 실행
- [ ] 각 라이브러리 버전이 출력됨
- [ ] 마지막 셀의 작은 PyTorch 텐서 연산이 동작함

## 문제 생기면
에러 메시지를 그대로 복사해서 AI agent에게 붙여넣고:
> "이 에러 원인이 뭐고, 어떻게 고치고, 왜 이런 에러가 나는지 설명해줘"
